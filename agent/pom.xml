<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.eventmap</groupId>
  <artifactId>event-map-agent</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>
  <name>event-map-agent</name>
  <description>Expose la topologie événementielle d'un service Spring AMQP sur /internal/event-manifest</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot.version>3.3.4</spring-boot.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!--
      Tout est `provided` : l'agent se greffe sur les dépendances que le service
      possède déjà. Il n'ajoute aucune dépendance transitive, ce qui est la
      condition pour l'imposer dans un parent pom sans discussion.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-amqp</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-configuration-processor</artifactId>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <!--
          Version épinglée : le plugin par défaut de Maven 3.8 est trop ancien
          pour interpréter maven.compiler.release et retombe sur source/target 5.
        -->
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <!--
            Le processeur d'annotations du module ne doit pas s'exécuter pendant
            la compilation du module lui-même : il n'est pas encore assemblé.
          -->
          <proc>none</proc>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
